<div class = "container">

  <p><%= link_to t('back'), products_path, class:"btn btn-outline-primary" %></p>
  <h3><%= t('.edit_product') %></h3>

  <%= render 'shared/errors', object: @product %>
  <%= render 'form', product: @product %>

</div>